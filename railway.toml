[build]
builder = "DOCKERFILE"
dockerfilePath = "keystore-server/Dockerfile"
dockerfileContext = "keystore-server"
watchPatterns = ["keystore-server/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
